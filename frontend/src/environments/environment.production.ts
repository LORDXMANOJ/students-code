export const environment = {
  production: true,
  // When deployed to production, set your backend URL (e.g. 'https://codetracker-backend.onrender.com') or leave empty for same-origin proxy
  apiUrl: ''
};
